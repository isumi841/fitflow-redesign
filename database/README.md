# Database

FitFlow uses **PostgreSQL** as the primary relational database.

`schema.sql` contains a small starter schema for the Activity 5 repository scaffold.
It is not intended to be the complete production database design.
